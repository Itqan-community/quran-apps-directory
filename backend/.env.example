# Django Settings
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Settings (PostgreSQL in Docker)
DB_NAME=quran_apps_db
DB_USER=postgres
DB_PASSWORD=postgres123
DB_HOST=localhost
DB_PORT=5432
USE_SQLITE=False

# Development Settings
# Set to True if you want to use SQLite instead of PostgreSQL for development
# WARNING: This is NOT recommended for consistency with production
# USE_SQLITE=True

# Django Debug Toolbar (optional)
# Install with: pip install django-debug-toolbar
# INSTALLED_APPS=debug_toolbar

# Email Settings (Development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# CORS Settings (Development)
# Add your frontend URL if different from default
# CORS_ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200

# Logging Level (Development)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Cache Settings (Development)
# Uses local memory cache by default
# CACHE_TIMEOUT=300

# Optional: Redis Settings (uncomment if using Redis in docker-compose.yml)
# REDIS_URL=redis://localhost:6379/0